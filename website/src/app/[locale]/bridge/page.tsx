import BridgePage from "@/components/bridge/BridgePage";

export const metadata = {
  title: "Celari Bridge — Sepolia ↔ Aztec Testnet",
  description: "Bridge ETH between Ethereum Sepolia and Aztec Testnet",
};

export default function Bridge() {
  return <BridgePage />;
}
